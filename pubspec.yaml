name: jet_vpn
description: "JET VPN - High Speed Secure VPN"
publish_to: 'none'
version: 1.7.0+17

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # هسته Xray 26.9.9 — پشتیبانی VLESS Encryption (mlkem768x25519plus)
  flutter_v2ray_client: ^3.5.0
  http: ^1.2.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.5
  percent_indicator: ^4.2.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
